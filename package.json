{
  "name": "space.u2re.service",
  "version": "1.0.0",
  "description": "CWS is a multi-device synchronization system with encrypted communication, clipboard sharing, and command forwarding capabilities.",
  "scripts": {
    "build": "./gradlew build",
    "dev": "./gradlew :app:installDebug",
    "assemble": "./gradlew :app:assembleDebug",
    "android:devices": "adb devices -l"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
